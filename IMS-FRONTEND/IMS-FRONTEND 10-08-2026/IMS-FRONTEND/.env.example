# Proxy Target for Vite Dev Server
# Replace with your local backend URL (e.g., http://localhost:5000 or http://localhost:7000)
# Default (if omitted) falls back to the remote ngrok URL in vite.config.js
VITE_API_PROXY_TARGET=http://localhost:5000
